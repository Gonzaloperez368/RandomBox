// FASE 1: todavía no hay funcionalidad, solo confirmamos que el archivo carga.
// Acá van a vivir las funciones de cada herramienta (generarNumero, mezclarLista, etc.)
// a medida que las construyamos fase por fase.

document.addEventListener("DOMContentLoaded", () => {
  console.log("RandomBox cargó correctamente.");
});
